# En local: déjalo vacío o no lo definas para usar el proxy de Vite (evita CORS y 401).
# Solo pon la URL completa si no usas proxy (ej. producción): https://api.ejemplo.com/api
# VITE_API_URL=http://localhost:5000/api
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
VITE_APP_NAME=EHR System
